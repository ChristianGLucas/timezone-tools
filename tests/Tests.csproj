<!-- Code generated by Axiom CLI; DO NOT EDIT. Axiom package: christiangeorgelucas/timezone-tools -->
<!-- Node unit tests (ADR-056). This project compiles the node implementations,
     their tests, and the AxiomContext, and compiles messages.proto via Grpc.Tools
     so the Gen.* message types are available — no node.proto / gRPC dispatcher is
     needed to unit-test a node function. `axiom test` runs `dotnet test` here.
     This file is REGENERATED from a template (by `axiom test`) — editing it by
     hand is futile. To add third-party NuGet dependencies your node code uses,
     declare them in axiom.yaml under build.nuget_deps; they are rendered into the
     declared-deps <ItemGroup> below so the node sources compiled here can resolve
     the library, exactly as the deploy service.csproj does. -->
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Google.Protobuf" Version="3.27.2" />
    <PackageReference Include="Grpc.Tools" Version="2.66.0" PrivateAssets="All" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.11.1" />
    <PackageReference Include="xunit" Version="2.9.0" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" PrivateAssets="All" />
  </ItemGroup>

  <ItemGroup>
    <!-- declared in axiom.yaml build.nuget_deps -->
    <PackageReference Include="NodaTime" Version="3.3.3" />
  </ItemGroup>

  <ItemGroup>
    <!-- The node implementations + their tests + the AxiomContext, compiled in
         place from the package tree one level up. -->
    <Compile Include="../nodes/**/*.cs" />
    <Compile Include="../Axiom/**/*.cs" />
    <!-- messages.proto → Gen.* message types (csharp_namespace = Gen). -->
    <Protobuf Include="../messages/*.proto" GrpcServices="None" ProtoRoot="../messages" />
    <!-- Imported cross-package messages → imports.<protoPkg> (rewritten in place
         by `axiom test`). Empty when the package declares no imports. -->
    <Protobuf Include="../imports/**/*.proto" GrpcServices="None" ProtoRoot="../imports" />
  </ItemGroup>

</Project>
